package Interfaces;
// Needed to create a new Interface, NOT a class
public interface Interface {
    // An interface's purpose is to be inherited from
    // We will never create an instance of an interface
    // Only use public methods with nothing defined within
    // Attributes need to be FINAL (constant; can't change value)
}
